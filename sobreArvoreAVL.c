//ARVORE AVL


    Arvore AVL é uma árvore binária de busca que é autoequilibrada
    onde a diferença de altura das subárvores direita e esquerda não
    pode ser maior que 1 para todos os nós.
    Essa diferença é chamada de fator de balanceamento;

    -> balance factor = height(left->subtree) - height(right->subtree)

    ROTAÇÕS AVL:
        -> left rotation
        -> right rotation
        -> left-right rotation
        -> right-left rotation

